# Quickstart: Scan Efficiency Pass

1. Ensure Spec Kit skills exist under `.cursor/skills/speckit-*`.
2. `npm test` in repo root.
3. Package: `npm run package` → `chaintrap-agent-shield-0.1.4.vsix`.
4. Install VSIX into VS Code/Cursor for dogfood; confirm status bar baseline completes and delta still fires on skill edits.
